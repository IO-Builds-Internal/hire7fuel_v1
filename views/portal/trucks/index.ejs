<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle %> | <%= config.brand.name %></title>
  <link rel="stylesheet" href="/css/styles.css?v=2.0">
  <link rel="stylesheet" href="/css/portal.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="admin-body">

  <div class="portal-layout">
    <%- include('../../partials/portal_nav') %>

    <div class="portal-dashboard">
      
      <div class="portal-card">
        <div class="portal-card-header">
          <h2>Truck Fleet Manager</h2>
          <a href="/portal/trucks/new" class="portal-btn portal-btn-primary">
            <i class="fas fa-plus"></i> Register Truck
          </a>
        </div>

        <p style="color: var(--color-muted); margin-bottom: 1.5rem; font-size: 0.95rem;">
          Track physical fleet assets, licensing plates, highway toll transponders, and schedules for PMVI and annual safety inspections.
        </p>

        <div class="portal-table-wrap">
          <table class="portal-table">
            <thead>
              <tr>
                <th>Unit #</th>
                <th>Make / Model / Year</th>
                <th>Active Plate</th>
                <th>Annual Safety</th>
                <th>PMVI Due Date</th>
                <th>Status</th>
                <th style="text-align: right;">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% if (trucks && trucks.length > 0) { %>
                <% trucks.forEach(function(t) { %>
                  <tr>
                    <td style="font-weight: 700; color: var(--color-white);">#<%= t.unit_number %></td>
                    <td><%= t.make %> <%= t.model %> (<%= t.year || '—' %>)</td>
                    <td style="font-family: monospace; font-weight: 600;"><%= t.activePlateNumber %></td>
                    <td>
                      <%- include('../../partials/expiry_badge', { expiry: t.annual_safety_expiry }) %>
                    </td>
                    <td>
                      <%- include('../../partials/expiry_badge', { expiry: t.pmvi_next_date }) %>
                    </td>
                    <td>
                      <span class="expiry-badge badge-green"><%= t.status %></span>
                    </td>
                    <td style="text-align: right; white-space: nowrap;">
                      <a href="/portal/trucks/<%= t.id %>" class="portal-btn portal-btn-secondary" style="padding: 0.4rem 0.8rem; font-size: 0.85rem; margin-right: 0.25rem;">
                        <i class="fas fa-eye"></i> View
                      </a>
                      <a href="/portal/trucks/<%= t.id %>/maintenance" class="portal-btn portal-btn-secondary" style="padding: 0.4rem 0.8rem; font-size: 0.85rem; border-color: var(--color-accent); color: var(--color-accent); margin-right: 0.25rem;">
                        <i class="fas fa-wrench"></i> Maintenance
                      </a>
                      <a href="/portal/trucks/<%= t.id %>/edit" class="portal-btn portal-btn-secondary" style="padding: 0.4rem 0.8rem; font-size: 0.85rem;">
                        <i class="fas fa-edit"></i> Edit
                      </a>
                    </td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr>
                  <td colspan="7" style="text-align: center; color: var(--color-muted); padding: 4rem 2rem;">
                    <i class="fas fa-truck" style="font-size: 2.5rem; color: rgba(20,160,119,0.1); margin-bottom: 1rem; display: block;"></i>
                    <p style="margin: 0; font-weight: 600; color: var(--color-white);">No trucks registered</p>
                    <p style="margin: 0; font-size: 0.85rem; margin-bottom: 1rem;">Add truck units to track registrations, safety certificates, and transponders.</p>
                    <a href="/portal/trucks/new" class="portal-btn portal-btn-primary">Register First Truck</a>
                  </td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>

    </div>
  </div>

</body>
</html>
